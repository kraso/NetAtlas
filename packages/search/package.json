{
  "name": "@netatlas/search",
  "version": "0.0.0",
  "private": true,
  "description": "NetAtlas — búsqueda: índice FTS5, facetas dinámicas y (F1) compilador DSL→SQL",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@netatlas/domain": "workspace:*",
    "@netatlas/data": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}