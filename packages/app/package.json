{
  "name": "@netatlas/app",
  "version": "0.0.0",
  "private": true,
  "description": "NetAtlas — casos de uso orquestadores: coordinan puertos del dominio y adaptadores (F1+ §26).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./casos/*": "./src/casos/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@netatlas/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
