{
  "name": "@netatlas/catalog-fetch",
  "version": "0.0.0",
  "private": true,
  "description": "NetAtlas — catalog:fetch (enriquecido web honesto: fetch con rate-limit + parse determinista + emision seed para revision)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "catalog:fetch": "tsx src/cli.ts"
  },
  "dependencies": {
    "@netatlas/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
